# Hanson-Tube: Execution & Context Tracker

## Summary of All Completed Milestones

- **Zero-Backend Architecture Validated**: React and Vite single-page app uses a static TypeScript data layer (`src/data/`) and Context API without external databases, servers, or runtime state management libraries.
- **Local Toolchain Modernization**: Standardized on `pnpm`, Vite with `@vitejs/plugin-react-swc`, Tailwind CSS (`@tailwindcss/vite`, `@theme`), TypeScript, ESLint Flat Config, and Husky + `lint-staged` pre-commit/pre-push hooks.
- **Visual Asset Suite Overhaul Completed**: Replaced legacy stock photos and flat word clouds across all 7 data layers (`skills.ts`, `projects.ts`, `work.ts`, `honors.ts`, `education.ts`, `certifications.ts`, `community.ts`) with standardized 16:9 abstract 3D dioramas, physical titanium/crystal artifacts, and grounded photorealistic operational scenes.
- **Modal Surface Shadow Orchestration**: Implemented `.modal-shadow` tokens in `src/index.css` and applied them to `ProjectModal.tsx` and `DetailModal.tsx`, replacing top/left specular highlights with lighter ambient shadows in both Light and Dark modes.
- **EmailJS Integration Secured**: `ContactPage.tsx` routes messages via `import.meta.env.VITE_EMAILJS_*` with anti-spam button throttling, client-side timestamp cooldown, and Sonner toast notifications.
- **Web Speech API Graceful Fallback**: Implemented Sonner toast notification and graceful fallback in `Header.tsx` when SpeechRecognition is unsupported or permissions are denied.
- **Comprehensive Unit Test Suite**: 24 passing Vitest test suites (103 unit and integration tests) covering `App.tsx`, `Header.tsx`, `Sidebar.tsx`, `SearchContext.tsx`, `ThemeContext.tsx`, `FormField.tsx`, `Section.tsx`, `Slideshow.tsx`, `LazyImage.tsx`, `useFocusTrap.tsx`, `usePWA.tsx`, `DetailModal.tsx`, `ProjectModal.tsx`, `SearchResults.tsx`, `ContactPage.tsx`, `EducationPage.tsx`, `WorkExperiencePage.tsx`, `ValuePage.tsx`, `FilterDropdown.tsx`, all 4 value charts, and `searchEngine.ts`.
- **Ecosystem & Entrepreneurial Journey Ingestion**: Ingested modern flagship venture products (CaroHans ERMS / HansonCreations Hub, MilCalc Mobile Suite, Gospel Games Platform, Unpack Travel Companion, Fretwork Guitar Capstone) framed as an entrepreneurial venture builder suite, cleaned up dead code exports, tokenized skill search matrices, and synchronized professional career achievements.
- **Performance Optimization & WebP Asset Pipeline**: Built automated Sharp conversion pipeline (`scripts/convert-assets.mjs`, `pnpm run assets:convert`) resulting in an 83.4% asset payload reduction (37.51 MB to 6.22 MB). Engineered reusable `LazyImage.tsx` primitive with native `loading="lazy"`, `decoding="async"`, smooth load transitions, and dark/light neumorphic skeleton placeholders across all 4 slideshow carousels, 4 page grids, and profile surfaces.
- **Interactive UX & Power-User Features**: Implemented global search hotkeys (`Ctrl+K`, `Cmd+K`, `/`, `Esc`) in `Header.tsx` and `SearchBar.tsx`, rich multi-layered indexing across all static datasets in `src/utils/searchableData.ts`, WCAG-compliant dialog focus containment with `useFocusTrap.ts` across `ProjectModal.tsx` and `DetailModal.tsx`, and horizontal keyboard arrow navigation in `Slideshow.tsx`. Extended unit test suite with zero ESLint errors and sub-2s production builds.
- **Core Modernization & Future-Proofing**: Full end-to-end upgrade to React / React DOM and TypeScript with strict typechecking (`tsc --noEmit`). Converted the entire static data layer, search indexing engine, React Contexts (`ThemeContext`, `SearchContext`), custom hooks (`useFocusTrap`, `usePWA`), UI primitives, modals, layout shell, page views, and test suites to strongly-typed `.ts` / `.tsx`. Integrated route-level code splitting via `React.lazy` and `Suspense` with neumorphic skeleton fallbacks, React native document metadata `<title>` / `<meta>`, and full Progressive Web App offline precaching via `vite-plugin-pwa` and Workbox.
- **Viewport Modal Portaling & Focus Modernization**: Refactored `DetailModal.tsx` and `ProjectModal.tsx` to render through `createPortal(..., document.body)` with background scroll locking (`document.body.style.overflow = "hidden"`) and cleanup. Cleaned up duplicate modal state in `SkillsSlideshow.tsx`, routing modal triggers through `Slideshow.tsx`.
- **Search Bar Red Focus & YouTube Red Highlight Tint**: Restored red focus rings and active states across `SearchBar.tsx` input, microphone button, and mobile search toggle in `Header.tsx`. Replaced generic yellow highlight (`bg-yellow-200`) in `SearchResults.tsx` with YouTube theme red tint (`bg-red-500/15 text-red-600 dark:bg-red-500/25 dark:text-red-400 font-semibold px-0.5 rounded-xs`).
- **Modal Content & Architecture Modular Refactoring**: Extracted reusable modular modal primitives (`ModalShell.tsx`, `ModalCARSection.tsx`, `ModalHighlightsGrid.tsx`, `CategorizedList.tsx`), reducing `ProjectModal.tsx` and `DetailModal.tsx` under 150 lines, modernized skill exposure badges, unified CAR blocks with supplementary details, enabled video previews, and added 10 unit tests in `DetailModal.test.tsx` and `ProjectModal.test.tsx`.
- **Humanized Content Refinement & Ecosystem Synchronization**: Refined project schemas in `src/data/projects.ts` for CaroHans ERMS, MilCalc Suite, Gospel Games, Unpack, Fretwork, and Portfolio Showcase with grounded descriptions and active verbs. Standardized Computer Vision into CAR schema, modernized tool tokens in `src/data/skills.ts`, and anchored corporate career achievements in `src/data/work.ts` on Lockheed Martin Space.
- **Performance Audit Remediation & Deploy Slimming**: Untracked `.env` with strict `.gitignore` coverage. Optimized `SearchContext` render hygiene using `useDeferredValue` and memoized provider value to prevent re-renders per keystroke. Throttled resize listener in `App.tsx` with `requestAnimationFrame`. Elevated avatar LCP with `fetchPriority="high"`. Slimmed Workbox SW precache from 7.6 MB to 413 KiB (CacheFirst runtime caching for imagery). Purged legacy source JPGs cutting deploy payload from 40.6 MB to 6.0 MB and compressed brand icons (1,585 KB → 126 KB).
- **Search & Microphone Dual-Mode Harmonization**: Calibrated `bevel-light-inset` in `src/index.css` to restore high-fidelity `origin/main` light depth. Refactored `SearchBar.tsx` and `Header.tsx` so Light Mode uses solid `bg-gray-200` with clean `bevel-light-inset` and neutral icons without border/ring hover noise, while Dark Mode preserves modern `#141416` recessed surfaces with `#4a4a4a` borders, red focus outlines, and red-accented active/listening states.
- **In-Memory Search Engine & Relevance Optimization**: Built custom zero-dependency `SearchEngine` with multi-token parsing, field-weighted scoring (Exact > Title > Tags > Subtitle > Summary > Content), tech alias expansions (e.g., C++, Next.js, AI/ML), Levenshtein typo tolerance, safe multi-token regex escaping, contextual snippet truncation, and unit testing across all test suites.
- **Vibe-Security & Threat Surface Audit**: Completed security review across all 9 Vibe-Security audit vectors (Secrets, Database/Cloud, Auth/RBAC, Rate Limiting, Payments, Client/PWA Bundle, AI/LLM Integration, Deployment Headers, Input Validation/ReDoS). Authored [review.md](./review.md) mapping security topology, confirming zero critical flaws, validating `escapeRegExp` ReDoS defenses, and establishing a remediation roadmap.
- **Security Hardening & Abuse Prevention**: Implemented 60-second client-side submission timestamp throttling in `localStorage` (`SUBMIT_COOLDOWN_MS = 60000`) on `ContactPage.tsx` with remaining time calculation, Sonner error toast on cooldown violation, EmailJS domain whitelist, and 4 passing unit tests in `ContactPage.test.tsx` (`VIBE-001`). Defined and staged edge security headers (CSP, HSTS, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, and `Permissions-Policy`) for Cloudflare edge proxying with `index.html` meta fallbacks (`VIBE-002`). Audited and documented Google Cloud Storage Uniform Bucket-Level Access and public read-only viewer role (`roles/storage.objectViewer`) constraints (`VIBE-004`).
- **Documentation & Tech Terminology Modernization**: Sanitized hardcoded and transient version numbers across all markdown documentation files (`docs/*.md`, `GEMINI.md`, `README.md`), static datasets (`src/data/skills.ts`, `src/data/projects.ts`), search index utilities (`src/utils/searchableData.ts`), and unit test mocks (`src/utils/searchEngine.test.ts`), replacing rigid version strings with standardized technology names while preserving semantic pins in `package.json`.
- **Subtle & Surgical TPM/Systems Leadership Alignment**: Preserved "Systems Engineer & Business Analyst" as the primary title across `ProfileSummaryCard.tsx` and `index.html` while subtly adding Active Secret Clearance and UC Berkeley M.S. Data Science credential badges. Enriched `src/data/work.ts` with Scrum Master delivery under emergency 50% manning cuts, Galaxy Assist 10+ defense program onboarding blueprints, ARB/CARB accredited CONOPS, and Qatar logistics ETL metrics. Enriched `src/data/projects.ts` with TPM stakeholder mediation ($1.0M funding, 800% adoption), 450-site rollout ($55.2k/mo = $662k/yr ROI, 0% errors), and $1.1M innovation platform. Enriched `src/data/skills.ts` with SAFe, Scrum Master, Cameo MBSE, and ARB/CARB, and expanded `src/utils/searchEngine.ts` aliases for instant TPM, Agile, and Governance query resolution with 103 passing unit tests across 24 test suites.
- **Lifetime Value & Quantitative/Qualitative Impact Dashboard**: Defined TypeScript interfaces in `src/types/data.ts` and authored `src/data/lifetimeValue.ts` capturing quantitative ROI metrics ($9.6M+ savings, 36x peak acceleration, 705k+ hours saved, 450 branch locations, 0.0% audit errors, 100% test coverage) and qualitative architectural leadership pillars. Built `FilterDropdown.tsx`, `ValueKpiGrid.tsx`, `ValueFilterBar.tsx`, and constructed `ValuePage.tsx` with dynamic routing and unit tests.
- **Value Dashboard Visual Overhaul**: Built interactive charts in `src/components/value/charts/` (`ValueRadarChart.tsx`, `ValueMultiplierBarChart.tsx`, `ValueTimelineAreaChart.tsx`, `ValueDomainDonutChart.tsx`), refactored `QualitativePillars.tsx` into a compact milestone drawer, assembled a responsive 2x2 grid in `ValuePage.tsx`, and added chart unit test suites.
- **USAF Career & Operational Leadership Ingestion**: Created centralized official system of record (`personal/sources/jobs/USAF.md`) synchronizing 13 official military documents (OPB, EPBs, LOE, Medals, ALS, CDCs, HSA). Enriched `src/data/work.ts` (MSC Officer 41A3 aeromedical readiness and TSgt NCOIC $92M CRF hub), `src/data/honors.ts` (Commendation Medals with 1st OLC, Achievement Medal, 22nd NAF Airman of the Year), `src/data/certifications.ts` (HSA and ALS PME), and `src/data/skills.ts` with military logistics and systems exposure badges. Expanded in-memory search engine aliases (`41a3`, `msc`, `hsa`, `als`, `crf`, `commendation`) with 100% test coverage and production build stability.
- **Broad Aggregate KPI Summary Buckets & Lifetime Experience Radar Calibration**: Reframed top summary cards into overarching lifetime buckets ($9.6M+ Cost Savings, 705k+ Reclaimed Hours, 36x Peak Acceleration, 450 Sites Operational Scale) that dynamically recalculate aggregate totals when filtered. Re-anchored radar axes around real lifetime depth and career longevity (Communication: 23 yrs, Systems & Hardware: 11 yrs, Supply Chain & Logistics: 10 yrs, Software & Monorepos: 7 yrs, Enterprise Automation: 6 yrs, Applied AI & Data Science: 4 yrs) with rich tooltip insights.
- **Value Page Refinement — Lifetime Savings & Time-Cost Metrics**: Reconciled `CAREER_ERAS` and chart `era` fields against accurate Work Experience timelines (Lockheed Martin Space June 2020 – Present, UCCS Nov 2019 – Dec 2021, USAF Sept 2015 – Present). Applied internal operations automation / enterprise stretch assignment framing across all 16 metrics + 6 qualitative pillars. Recomputed donut distribution on impact-footprint counts (Enterprise 50%, Defense & Space 25%, Academic & Community 13%, Military Operations 6%, Proprietary Ecosystem 6%). Added `FINANCIAL_LEDGER` ground-truth dataset driving filtered dollar totals.
- **Timeline & Multiplier Chart Restoration**: Restored dual-series line/area chart (`ValueTimelineAreaChart`) plotting cumulative lifetime savings ($M) and reclaimed hours from 2015 to present day across 4 grounded career eras ($1.09M → $1.84M → $7.85M → $9.61M; 15k → 65k → 240k → 705k hrs). Rebuilt `ValueMultiplierBarChart` around cycle-time acceleration factors (36x OAR reconciliation, 18x ETL refresh, 10x SNO builds, 2x aeromedical pass). Re-grounded executive savings headline to "$9.6M+" to match the financial ledger sum and timeline endpoint.
- **Executive KPI Card Grounding**: Audited all 4 Value page KPI cards against source work. Savings card cites $5M Boeing retread, $1.1M innovation engine, and 450-site automation. Hours card dynamically aggregates filtered annual hours. Peak velocity card corrected to true measured peak "36x" (OAR nightly reconciliation, 3 hrs → 5 min). Scale card cites $662k/yr savings and 0.0% audit errors.
- **Education Modal Context & Academic Rigor Ingestion**: Ingested academic context from 8 graduate course portfolios (`../personal/sources/gradschool/`) and 27 undergraduate course portfolios (`../personal/sources/undergrad/`). Expanded `src/data/education.ts` with categorized curriculum blocks, key research highlights (Fretwork AMT, multivariate LSTM sequence forecasting), academic metrics (4.0 GPA, 3.67 GPA, 7x Dean's List), and tool exposure badges. Extended `EducationDetails` in `src/types/data.ts` with `highlights` and `exposure`. Updated search aliases and added unit tests in `EducationPage.test.tsx`.
- **Content Humanization & Voice Refinement**: Applied the humanize skill framework across the entire static data layer (`projects.ts`, `work.ts`, `lifetimeValue.ts`, `education.ts`, `honors.ts`, `community.ts`, `certifications.ts`, `skills.ts`). Stripped AI buzzwords and formulaic resume phrasing, replacing them with direct, engineer-written narratives while preserving all data structures, metrics ($9.6M+ ROI, 705k+ hrs saved, 36x acceleration, 450 sites), AFSCs, clearance levels, program names, and IDs. Added concise production comments (~every 10–15 lines).
- **Centralized Theme, Icon Registry & Strict ESLint Enforcement**: Built single source of truth theme engine in `src/theme/theme.ts` with `tokens.light` (`brandRed: #db2926`) and `tokens.dark` (`brandRed: #ef4444`), `getAlphaColor`, `CHART_PALETTE`, and `NEUMORPHIC_SHADOWS`. Enriched `ThemeContext` and `useTheme()` hook with typed `tokens` and `colors`. Unified iconography under `ICONS` semantic constants and `<Icon />` primitive. Migrated all consumer components away from direct `react-icons/*` imports. Configured strict ESLint guardrails in `eslint.config.js` (`no-restricted-imports` and `no-restricted-syntax`).
- **Zero-Overhead Tailwind Class Deduplication & Architectural Alignment**: Consolidated strongly typed, immutable (`as const`) Tailwind compound class recipes (`UI_SURFACES`, `UI_TYPOGRAPHY`, `UI_BADGES`, `UI_BUTTONS`, `UI_INPUTS`) into `src/theme/theme.ts`. Refactored cards, grids, modals, and interactive controls across pages to consume the centralized style definitions.
- **Theme Harmonization, Navigation Chrome Consistency & Chart Preview Standardization**: Aligned `SearchBar.tsx`, `Header.tsx`, and `Sidebar.tsx` with origin feature branch aesthetics via semantic `@theme` tokens (`--color-dark-well: #141416`, `--color-dark-well-hover: #1e1e22`, `--color-dark-header: #181818`). Harmonized `Sidebar.tsx` background to `dark:bg-dark-header` (`#181818`) to match `Header.tsx`. Restored form field dark well background (`#242529`) and deep dual-vector neumorphic inset shadows in `FormField.tsx`. Ingested "Open Heavens Media Team Lead" into `src/data/community.ts` and indexed search synonyms. Standardized all four value chart hover preview windows to uniform `mt-3 min-h-[52px] p-2.5` dimensions with rich 2-line structured headers, percentage badges, and sector narrative context.
- **Enterprise Terminology & Narrative Sanitization**: Sanitized commercial retail banking terms across datasets (`lifetimeValue.ts`, `projects.ts`, `ValueKpiGrid.tsx`, `personal/synthesis/value.md`), establishing the neutral Enterprise Terminology Rulebook in `personal/GEMINI.md` (e.g., Enterprise Facilities / Regional Sites, Capital Pipelines / Allocation Queues, Secure Access Credentials, Portfolio Operations).
- **Standardized Domain Sectors & Career Eras**: Standardized 5 discrete Domain Sectors (`Defense & Space`, `Enterprise Automation`, `Military Logistics & Readiness`, `Software Solutions`, `Academic & Research`) and 4 functional Career Eras (`2015 – 2019: Logistics & Supply Chain`, `2019 – 2021: Software Dev & Expeditionary Logistics`, `2020 – 2024: Systems Engineering & Program Management`, `2024 – Present: Infrastructure & Enterprise Automation`). Expanded UCCS full-stack web and database migration quantitative metric (`val-17`) under Academic & Research.
- **Full-Scale Prose Humanization & Voice Refinement**: Executed `/humanize` pass across all datasets (`projects.ts`, `work.ts`, `lifetimeValue.ts`, `education.ts`, `honors.ts`, `community.ts`, `certifications.ts`, `skills.ts`), removing AI clichés, promotional puffery, and passive `-ing` chains in favor of direct, engineer-written narratives while preserving all quantitative data points and 100% test pass rate.
- **TPM Skill Hierarchy, Red Ellipsis Harmonization & Work Experience Card Synthesis**: Reorganized skill categories and internal priorities in `src/data/skills.ts` to lead with the 4 core TPM pillars from `../personal/Gemini.md` (Project & Program Management, Cloud & DevOps, BI & Data Analytics, Programming, followed by IT & Systems, Adobe Suite, Electronics, and Languages). Harmonized brand red `...` ellipsis rendering across `searchUtils.tsx` and `SkillsSlideshow.tsx`. Synthesized all verified TPM and operational metrics into clean, structured key-value front summary cards (`Role:`, `Focus:`, `Impact:`, `Stack:`, `Projects:`, `Leadership:`, `Operations:`) in `src/data/work.ts` and `WorkExperiencePage.tsx` with bold company titles and crisp label styling matching the beloved `origin/main` aesthetic. Verified 100% test pass rate (103 tests across 24 suites), zero ESLint errors, and clean production build.
- **Grounded Engineering Voice, Skills Architecture & Complete Verification**: Refined all 8 skill categories in `src/data/skills.ts`, eliminating overreach, forced metrics, and artificial resume keyword-stuffing in favor of clear, authentic engineering and technical program management capabilities. Structured `modalDetails.details` into clean, labeled competency sentences and added sorted `exposure` badge lists across all skill categories. Sorted all exposure arrays and content bullet points across `work.ts`, `education.ts`, `honors.ts`, and `certifications.ts` by architectural and operational hierarchy. Harmonized `SkillsSlideshow.tsx` with origin red ellipsis (`text-red-500 dark:text-red-400`). Successfully executed full `/verify` sequence (ESLint 0 errors, TypeScript 0 errors, Vitest 103/103 tests passing, and Vite production bundle generated cleanly in 2.09s).
- **Site-Wide Prose Humanization & Voice Audit**: Executed `/humanize` audit across all datasets and components (`projects.ts`, `work.ts`, `lifetimeValue.ts`, `education.ts`, `honors.ts`, `community.ts`, `certifications.ts`, `skills.ts`, `HomePage.tsx`, `ProfileSummaryCard.tsx`). Replaced lingering promotional buzzwords ("showcasing" → "covering", "streamlined" → "simplified", "Community Empowerment" → "Community Programs") with authentic, direct, engineer-written prose while preserving all quantitative data points, titles, and 100% test pass rate.
- **Architectural Documentation Suite & Standards Enforcement**: Maintained documentation and standalone Mermaid diagrams across [architecture.md](./architecture.md), [PRD.md](./PRD.md), [backend.md](./backend.md), [hanson-tube.md](./hanson-tube.md), [images.md](./images.md), [review.md](./review.md), [testing.md](./testing.md), [lint.md](./lint.md), and [theme.md](./theme.md), enforcing relative file paths across all documentation artifacts.
- **Recommended Search Results & Empty State Architecture**: Engineered category-diverse recommendation engine in `SearchEngine.ts` (`getRecommendations`) and integrated with `SearchContext.tsx`, `SearchBar.tsx`, and `SearchResults.tsx`. Surfaced curated flagship ventures (Gospel Games, Unpack, MilCalc), core TPM skills, military honors (22nd NAF Airman of the Year), and career highlights in a dedicated "Recommended Topics" panel upon search focus with empty query. Added live search on keystrokes, empty-query fallback messaging, `FaStar`/`FaCompass` icon registry expansion, and 26 passing Vitest test suites (113 tests) with zero ESLint errors and sub-2s production builds.
- **Documentation Suite Refinement & Technical Verification (`/refinedocs`)**: Cross-referenced all 12 markdown documents against the codebase Single Source of Truth. Reconciled test metrics across [README.md](../README.md), [testing.md](./testing.md), [review.md](./review.md), and [checklist.md](./checklist.md) to 26 passing suites (113 tests). Enriched [PRD.md](./PRD.md), [architecture.md](./architecture.md), [backend.md](./backend.md), and [hanson-tube.md](./hanson-tube.md) with smart recommendation workflows and `lifetimeValue.ts` coverage. Synchronized [images.md](./images.md) certifications catalog to all 6 cards, validated [lint.md](./lint.md) and [theme.md](./theme.md) against Flat Config and `@theme` tokens, and verified 100% test pass rate with zero ESLint warnings.
- **Comprehensive Architectural Modernization, Hook Extraction, O(1) Indexing & Strict Type Safety**:
  - **Strict Type Safety & a11y Hardening**: Created `src/types/speech.d.ts` declaring standard W3C Web Speech API interfaces (`SpeechRecognition`, `SpeechRecognitionEvent`, `SpeechRecognitionErrorEvent`), eliminating all `any` casts from `Header.tsx` and `Header.test.tsx`. Added `aria-label` and `aria-current="page"` to `Sidebar.tsx` navigation buttons and theme toggle, explicit `aria-label="Search portfolio"` on `SearchBar.tsx`, and `aria-invalid` with `aria-describedby` linking inputs and textareas to error alert paragraphs in `FormField.tsx`.
  - **Pragmatic Component Modularization & Custom Hooks**: Extracted `useVoiceSearch.ts` and `useSearchHotkeys.ts` from `Header.tsx` (dropping from 355 to 164 lines); extracted `PageSkeleton.tsx` and `useResponsiveSidebar.ts` from `App.tsx` (dropping from 193 to 110 lines); extracted `useContactForm.ts` from `ContactPage.tsx` (dropping from 155 to 92 lines); and extracted pure Catmull-Rom spline calculations and linear coordinate projection into `chartUtils.ts` with subcomponents `TimelineHeader.tsx`, `TimelineDetailBox.tsx`, `DonutLegend.tsx`, and `DonutDetailBox.tsx` from `ValueTimelineAreaChart.tsx` and `ValueDomainDonutChart.tsx`.
  - **DRY Code Consolidation**: Engineered `<SummaryTextLines />` Single Source of Truth for card summary colon-parsing, migrating `ProjectsPage.tsx`, `WorkExperiencePage.tsx`, `EducationPage.tsx`, and `HonorsPage.tsx`; extracted internal `CategorizedListItem` in `CategorizedList.tsx`; and unified `ProjectModalProps` on `ProjectDetails`.
  - **Data Structures, Indexing & Performance**: Built in-memory `projectLookupMap` (`Map<string, ProjectDetails>`) and `findProjectByLabel` in `src/data/projects.ts` replacing O(N) array searching with O(1) map indexing; standardized `SkillCategory` with static `summary?: string[]` across all 8 competency categories in `src/data/skills.ts`, dropping complex runtime nested sorting and character-counting loops in `SkillsSlideshow.tsx`; memoized filter calculations in `ValuePage.tsx` (`useMemo`, `useCallback`) and donut segments in `ValueDomainDonutChart.tsx`; memoized context provider value in `ThemeContext.tsx` to prevent tree re-renders; and pre-merged `highPriorityTokens` / `medPriorityTokens` in `SearchEngine.ts` to eliminate array allocations in hot search loops.
  - **Unit Test Coverage Expansion**: Added 7 new test suites (`chartUtils.test.ts`, `SummaryTextLines.test.tsx`, `useSearchHotkeys.test.ts`, `useResponsiveSidebar.test.ts`, `useContactForm.test.ts`, `useVoiceSearch.test.ts`, `CategorizedList.test.tsx`), bringing total test coverage to 33 test files and 140 passing tests with 100% pass rate, zero ESLint errors, zero TypeScript errors, and verified production builds.

---

## Active Roadmap: To-Do Items

### Phase 1: Strict Type Safety & Accessibility (a11y) Hardening

- [x] **[TYP-1] Strict Web Speech API Type Definitions**: Create `src/types/speech.d.ts` declaring standard W3C Web Speech API interfaces (`SpeechRecognition`, `SpeechRecognitionEvent`, `SpeechRecognitionErrorEvent`) to eliminate all 5 `// eslint-disable-next-line @typescript-eslint/no-explicit-any` workarounds in `Header.tsx`.
- [x] **[A11Y-1] Navigation & Control ARIA Attributes**: Add explicit `aria-label` and `aria-current={activePage === item.name ? "page" : undefined}` on `Sidebar.tsx` navigation buttons and theme toggle; add explicit `aria-label="Search portfolio items"` on `SearchBar.tsx` input.
- [x] **[A11Y-2] Form Field Accessibility & Error Association**: Add `aria-invalid={Boolean(errorObj)}` and `aria-describedby` error element linking to input and textarea controls in `FormField.tsx`.

### Phase 2: Pragmatic Component Modularization & Hook Extraction

- [x] **[MOD-1] Header & Search State Decomposition**: Extract speech recognition lifecycle into `useVoiceSearch.ts` and global hotkey bindings (`Ctrl+K`, `Cmd+K`, `/`, `Esc`) into `useSearchHotkeys.ts`, streamlining `Header.tsx` (355 lines) into a lean presentation shell.
- [x] **[MOD-2] App Shell & Responsive Layout Decomposition**: Extract `PageSkeleton` into `src/components/ui/PageSkeleton.tsx` and window resize / click-outside / auto-collapse logic into `useResponsiveSidebar.ts`, bringing `App.tsx` (193 lines) to < 100 lines.
- [x] **[MOD-3] Contact Form Logic & Cooldown Extraction**: Extract EmailJS dispatch, client-side timestamp cooldown (`SUBMIT_COOLDOWN_MS`), and submission statuses into a reusable `useContactForm.ts` hook, keeping `ContactPage.tsx` (155 lines) focused strictly on layout.
- [x] **[MOD-4] Interactive Value Chart Geometry Extraction**: Extract Catmull-Rom smoothing math and SVG coordinate utilities from `ValueTimelineAreaChart.tsx` into `chartUtils.ts`, and extract subcomponents (`TimelineHeader.tsx`, `TimelineDetailBox.tsx`, `DonutLegend.tsx`), reducing `ValueTimelineAreaChart.tsx` (330 lines) and `ValueDomainDonutChart.tsx` (235 lines) into concise visual shells.

### Phase 3: DRY Code Consolidation & Rendering Primitives

- [x] **[DRY-1] Unified Card Summary Text Component**: Create a reusable `<SummaryTextLines lines={item.summary} />` primitive in `src/components/ui/` to eliminate duplicate colon-splitting parsing across `ProjectsPage.tsx`, `WorkExperiencePage.tsx`, `EducationPage.tsx`, and `HonorsPage.tsx`.
- [x] **[DRY-2] Modal Item Categorized List Deduplication**: Consolidate duplicate label colon-splitting and external project button rendering in `CategorizedList.tsx` into a single helper/subcomponent.
- [x] **[DRY-3] Project Modal Interface Deduplication**: Remove redundant duplicate object union in `ProjectModalProps` in `ProjectModal.tsx`, unifying on `ProjectDetails`.

### Phase 4: Data Structures, Indexing & Performance Optimization

- [x] **[DAT-1] O(1) In-Memory Project Lookup Map**: Build a pre-indexed `Map<string, ProjectDetails>` in `src/data/projects.ts` to replace repeated O(N) `.find()` searches and redundant `.toLowerCase()` calls in `CategorizedList.tsx`.
- [x] **[DAT-2] SkillCategory Schema & Summary Standardization**: Standardize `SkillCategory` by adding static `summary: string[]` (matching `ProjectItem`, `WorkExperience`, `EducationItem`, etc.) and typing `details` consistently, eliminating dynamic array slicing and sorting on every render in `SkillsSlideshow.tsx`.
- [x] **[PERF-1] Value Dashboard Memoization & Computation Hygiene**: Wrap `filteredMultipliers`, `filteredRadar`, and `filteredSavingsTotal` in `ValuePage.tsx` with `useMemo`, and memoize SVG donut segment calculations in `ValueDomainDonutChart.tsx`.
- [x] **[PERF-2] Theme Context Provider Value Memoization**: Wrap the context value object in `ThemeContext.tsx` with `useMemo` to eliminate unnecessary full-tree re-renders when theme state is unchanged.
- [x] **[PERF-3] Search Engine Token Allocation Optimization**: Pre-concatenate high-priority token arrays (`doc.highPriorityTokens`) during indexing in `SearchEngine.ts` to avoid re-allocating arrays inside hot query token loops.

### Phase 5: Continuous Verification & E2E Validation

- [x] **[TST-1] Expanded Unit & Hook Test Coverage**: Add unit tests for newly extracted hooks (`useVoiceSearch`, `useSearchHotkeys`, `useResponsiveSidebar`, `useContactForm`) and utility modules (`chartUtils`, `SummaryTextLines`, `CategorizedList`).
- [ ] **[TST-2] Playwright End-to-End Test Suite**: Implement Playwright tests verifying end-to-end user journeys (searching, filtering, responsive navigation, and modal flows).
- [x] **[VER-3] Visual Regression & Quality Verification**: Validate zero broken images, UI layout stability, ESLint rules, TypeScript strictness, Vitest tests (`pnpm test:run`), and Vite production build (`pnpm build`).

### Phase 6: Production Readiness & Pre-Deployment Pipeline

- [ ] **[DEP-1] Production Bundle Optimization & Asset Audit**: Audit Vite production bundle chunking, asset hashing, and PWA precache manifest integrity.
- [ ] **[DEP-2] Edge Header & CDN Staging Verification**: Verify Cloudflare DNS, edge security headers, and GitHub Pages custom domain routing.
- [ ] **[DEP-3] Cross-Browser & Device Verification**: Verify touch gestures, voice speech recognition, and keyboard shortcuts across Chrome, Safari, Firefox, and mobile viewports.
