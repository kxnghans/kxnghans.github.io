export const FEATURE_FLAGS = {
  /**
   * Toggles visibility of the Value & Lifetime ROI Intelligence page.
   * When set to false, the Value navigation icon is removed from the sidebar
   * and the route is guarded to prevent access in production.
   */
  showValuePage: true,
};
